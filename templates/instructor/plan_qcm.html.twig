{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link href="{{asset('css/instructor/plan_qcm.css')}}" rel="stylesheet">
    <link href="{{asset('build/app.css')}}" rel="stylesheet">
    <link href="{{asset('css/instructor/ckeditor_overwrite.css')}}" rel="stylesheet">
{% endblock %}

{% block title %}{{ 'title.planQcm'|trans }}{% endblock %}

{% block body %}
    <h1>- {{ 'title.planQcm'|trans }} -</h1>
    <div class="subContainer">
        <form action="{{ path('instructor_distribue_qcm') }}" method="post">
            <section class="sectionChoice">
                <p>
                    <span class="step">{{ 'title.step1'|trans }}</span> {{ 'title.chooseSession'|trans }}
                </p>
                <ul class="sessionChoice">
                    {% for session in instructorSessions %}
                        <li class="liSession">
                            <input class="nameSession" id="{{ session.name }}" type="radio" name="session" value="{{ session.id }}"><label for="{{ session.name }}">{{ session.name }}</label>
                        </li>
                    {% endfor %}
                </ul>
            </section>
            <section class="sectionChoice">
                <p>
                    <span class="step">{{ 'title.step2'|trans }}</span> {{ 'module.choice'|trans }}
                </p>
                <div class="backgroundEtape">
                    <select class="selectVarFond" id="module-choice" name="module">
                        <option value="0">{{ 'module.beforeChoice'|trans }}</option>
                    </select>
                </div>
            </section>
            <section class="sectionChoice">
                <p>
                    <span class="step">{{ 'title.step3'|trans }}</span> {{ 'qcm.choice'|trans }}
                </p>
                <div class="backgroundEtape">
                    <select class="selectVarFond" id="qcm-choice" name="qcm">
                        <option value="0">{{ 'module.selectModuleToChooseQcm'|trans }}</option>
                    </select>
                </div>
            </section>
            <section class="sectionChoice">
                <p>
                    <span class="step">{{ 'title.step4'|trans }}</span> {{ 'qcm.chooseStudentsQcm'|trans }}
                    <ul id="qcm-students">
                        <li>{{ 'title.studentListWillAppear'|trans }}</li>
                    </ul>
                </p>
            </section>
            <section class="sectionChoice">
                <p>
                    <span class="step">{{ 'title.step5'|trans }}</span> {{ 'title.chooseDates'|trans }}
                </p>
                <div class="allDate">
                    <div class="date">
                        <label for="start-time">{{ 'title.accessibleDate'|trans }}</label>
                        <input id="start-time" name="start-time" type="datetime-local">
                    </div>
                    <div class="date">
                        <label for="end-time">{{ 'title.limitDateQcm'|trans }}</label>
                        <input id="end-time" name="end-time" type="datetime-local">
                    </div>
                </div>
            </section>
            <button class="valid" type="submit">{{ 'title.distributeQcmToStudents'|trans }}</button>
        </form>
    </div>
{% endblock %}

{% block javascripts %}
    <script src="https://code.jquery.com/jquery-3.6.0.js" integrity="sha256-H+K7U5CnXl1h5ywQfKtSj8PCmoN9aaq30gDh27Xc0jk=" crossorigin="anonymous"></script>
    <script type="text/javascript" src="{{ asset('js/instructor/qcmPlanner.js') }}"></script>
{% endblock %}