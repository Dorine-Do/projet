{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link href="{{asset('build/app.css')}}" rel="stylesheet">
    <link href="{{asset('css/instructor/distributed_qcms.css')}}" rel="stylesheet">
    <link href="{{asset('css/instructor/ckeditor_overwrite.css')}}" rel="stylesheet">
{% endblock %}

{% block title %}{% endblock %}

{% block body %}
    <h1>- {{ 'title.qcmPlan'|trans }} -</h1>

    <p class="align">
        <span class="circle orangeCircle">1</span> {{ 'session.choice'|trans }}
    </p>
    <div class="firstSelect">
        <select id="session-choice" name="session">
            <option value="0">{{ 'session.sessionModule'|trans }}</option>
            {% for session in sessions %}
                <option value="{{ session.id }}">{{ session.name }}</option>
            {% endfor %}
        </select>
    </div>


    <p class="align">
        <span class="circle orangeCircle">2</span> {{ 'module.choice'|trans }}
    </p>
    <div class="secondSelect">
        <select id="module-choice" name="module">
            <option value="0">{{ 'module.plsChoice'|trans }}</option>
        </select>
    </div>

    <div class="overflowX">
        <div id="qcms-module">
            <ul id="div-legend">
                <li class="legendTypeDifficulty">{{ 'title.module'|trans }}</li>
                <li class="legendDate">{{ 'title.date'|trans }}</li>
                <li class="legendModule">{{ 'title.type'|trans }}</li>
                <li class="legendNote">{{ 'title.difficulty'|trans }}</li>
            </ul>
        </div>
    </div>
    <div id="students-qcm">


    </div>

{% endblock %}

{% block javascripts %}
    <script>
        let decouvre = "{{asset('build/images/arbre_decouvre.svg')}}"
        let explore = "{{asset('build/images/arbre_explore.svg')}}"
        let maitrise = "{{asset('build/images/arbre_maîtrise.svg')}}"
        let domine = "{{asset('build/images/arbre_domine.svg')}}"
    </script>
    <script type="text/javascript" src="{{ asset('js/instructor/distributedQcms.js') }}"></script>

{% endblock %}